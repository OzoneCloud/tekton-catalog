params:
- default: orka-token
  description: 'The name of the secret holding the authentication token used to access
    the Orka API.

    '
  name: orka-token-secret
  type: 1
- default: token
  description: 'The name of the key in the Orka token secret, which holds the authentication
    token.

    '
  name: orka-token-secret-key
  type: 1
- default: orka-vm-name
  description: 'The name of the config map, which stores the name of the generated
    VM configuration.

    '
  name: orka-vm-name-config
  type: 1
- default: vm-name
  description: 'The name of the key in the VM name config map, which stores the name
    of the generated VM configuration.

    '
  name: orka-vm-name-config-key
  type: 1
- default: docker.io/macstadium/orka-tekton-runner:2020-10-23-a93757dc-0.1@sha256:e8ed3370dcb91cdb8bcd4e9a7e9f6652879d80acdab9644cda69a98cd8c93339
  description: 'The name of the docker image which runs the task step.

    '
  name: orka-image
  type: 1
visibility:
  showToUser: true
